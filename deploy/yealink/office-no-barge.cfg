#!version:1.0.0.1
# Merge into the provider's authoritative configuration for selected office phones.
# Pilot and verify emergency coverage first: see docs/CALL_INTERRUPTION.md.
# Busy phones ignore multicast pages; idle phones retain their subscriptions.
multicast.receive_priority.priority = 0
